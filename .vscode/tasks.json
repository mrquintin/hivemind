{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Sync to GitHub (after Keep all)",
      "type": "shell",
      "command": "${workspaceFolder}/scripts/sync-to-github.sh",
      "group": "none",
      "presentation": { "reveal": "always", "panel": "shared" }
    }
  ]
}
