You are a principal-level systems architect + full-stack engineer working inside this repository.
You must complete the remaining work on the Hivemind v2 debate engine with production-grade precision.
Assume ambiguous requirements are unacceptable. Prefer explicit contracts, invariants, and deterministic behavior.

===============================================================================
0) CURRENT IMPLEMENTATION STATE (ALREADY COMPLETED — DO NOT REDO)
===============================================================================

The following work has been completed and tested:

Backend core (cloud/hivemind_core/):
- types.py: AnalysisMode, EffortLevel, TerminationReason, RecommendationStatus enums.
  BudgetUsage, RepairStats, BudgetExhausted. EFFORT_DEFAULTS. New fields on
  HivemindInput (analysis_mode, effort_level, budget caps, repair/stagnation params),
  HivemindOutput (termination_reason, budget_usage, mode_used, repair_stats),
  Recommendation (status, repair_history, partial_scoring), AggregatedSolution (cluster_evidence).
  StorageInterface includes get_documents_for_knowledge_bases(). VectorStoreInterface
  includes document_ids filter param.

- debate.py: Refactored into explicit subroutines:
  run_simple_mode(), run_full_mode(), apply_practicality_scoring(),
  repair_failed_recommendations(), cluster_solutions_monitor_v2() (3-stage pipeline
  with embedding similarity Stage 1, LLM adjudication Stage 2, canonical form
  extraction + contradiction detection + structured merge Stage 3).
  _BudgetGuard (centralized budget enforcement), _StagnationDetector.
  _make_audit_details() helper defined. _parse_solution_reasoning() splits
  solution/reasoning. _parse_critique_response() extracts strengths/weaknesses/
  suggestions. _create_revision_prompt() includes revision_strength parameter as
  percentage in prompt text. apply_practicality_scoring() injects
  practicality_criticality as percentage into feasibility evaluation prompt.
  Partial scoring (F2): BudgetExhausted during scoring sets rec.partial_scoring=True
  and emits partial_practicality_scoring audit event. Streaming uses thread+queue
  pattern for real-time event yielding. _extract_canonical_form(),
  _detect_contradiction(), _merge_canonical_forms() implement full canonical
  pipeline. _get_document_token_counts() calls storage.get_documents_for_knowledge_bases().
  _create_dynamic_units() distributes documents (not KB IDs) into units.

- agents.py: build_theory_prompt() includes SOLUTION/REASONING format instruction.
  execute_agent() supports document_ids for scoped RAG retrieval.

- engine.py: Mode/effort pass-through to debate functions.

- interfaces.py: Cleaned up — re-exports from types.py, no duplicate definitions.
  InMemoryStorage testing helper retained.

Backend API (cloud/app/):
- schemas/analysis.py: analysis_mode, effort_level, budget override fields in request.
  termination_reason, BudgetUsageOut, RepairStatsOut, mode_used in response.
  RecommendationOut has status and repair_history. Enum validators.

- routers/analysis.py: _build_hivemind_input() maps all new fields.
  Backward-compatible: missing analysis_mode defaults to "simple".

- routers/agents.py: Accepts optional status query parameter for filtering.

- routers/knowledge_bases.py: GET /knowledge-bases/density-bounds endpoint exists.

- models/agent.py: use_case_profile column exists.

- models/knowledge_base.py: decision_types JSONB column exists.

- deps.py: get_current_user() and get_current_client() JWT dependencies defined.

- adapters/storage.py: PostgresStorage implements get_documents_for_knowledge_bases()
  with real token count aggregation from TextChunk table.

- ws/: Duplicate analysis.py removed. Only handlers.py remains.

Client (client/src/):
- api/client.ts: Updated AnalysisRequest, Recommendation, AnalysisResult types.
  BudgetUsage, RepairStats, RepairHistoryEntry interfaces. Payload builders.

- App.tsx: Mode selector (simple/full), effort selector (low/medium/high) as
  primary controls. Density/similarity/revision moved to "Experimental" advanced
  panel. Repair phase step. Output modal shows termination reason, budget usage,
  repair stats, per-recommendation status badges and repair history.

- styles.css: Mode/effort selectors, experimental badge, termination banner,
  repair summary, status badges, repair history styling.

Tests (cloud/tests/):
- test_types.py: 20 tests for new enums, defaults, backward compat.
- test_debate_helpers.py: 19 tests for parsers, budget guard, stagnation detector.
- test_debate_modes.py: 13 tests for mode branching, repair loop, budget enforcement,
  backward compat, streaming events.
- test_simulations.py: 4 tests unchanged (no regression).

Evaluation (cloud/scripts/):
- benchmark_modes.py: Full evaluation harness with MockLLM, runs simple + full
  mode across 20 benchmark prompts, collects metrics.
- benchmark_prompts.json: 20 prompts across 4 decision categories.
- benchmark_report.md: Generated markdown report with per-prompt and aggregate stats.

Primary implementation files (for reference):
- cloud/hivemind_core/debate.py
- cloud/hivemind_core/types.py
- cloud/hivemind_core/engine.py
- cloud/hivemind_core/agents.py
- cloud/hivemind_core/interfaces.py
- cloud/app/routers/analysis.py
- cloud/app/routers/agents.py
- cloud/app/routers/knowledge_bases.py
- cloud/app/models/agent.py
- cloud/app/models/knowledge_base.py
- cloud/app/adapters/storage.py
- cloud/app/deps.py
- cloud/app/schemas/analysis.py
- client/src/App.tsx
- client/src/api/client.ts
- client/src/styles.css
- cloud/tests/test_debate_modes.py
- cloud/tests/test_debate_helpers.py
- cloud/tests/test_types.py
- cloud/scripts/benchmark_modes.py

===============================================================================
1) REMAINING WORK — AUDIT EVENT CONVENTION: USE _make_audit_details EVERYWHERE
===============================================================================

STATUS: The _make_audit_details() helper is defined at debate.py:50-65 but is
never actually called. All audit events in the file still use inline dicts,
most of which include "event_version": "v2" but are missing mode, round,
run_id, and timestamp_iso fields.

REQUIREMENT:
Replace every inline AuditEvent details dict in debate.py with a call to
_make_audit_details(mode, run_id, round=N, **extra). This includes audit events
in:
- run_simple_mode() (debate_start, initial_solutions_generated, etc.)
- run_full_mode() (debate_start, debate_round_start, debate_round_complete,
  global_restart, etc.)
- repair_failed_recommendations() (repair_iteration)
- cluster_solutions_monitor_v2() (monitor_v2_clustering, monitor_parse_fallback)
- apply_practicality_scoring() (partial_practicality_scoring)

MODE and RUN_ID must be threaded through to all functions that emit audit events.
Currently run_simple_mode and run_full_mode have run_id and know the mode, but
the helper functions they call (apply_practicality_scoring, repair_failed_recommendations,
cluster_solutions_monitor_v2) do not receive these parameters.

FIX:
1. Add mode: str and run_id: str parameters to apply_practicality_scoring(),
   repair_failed_recommendations(), and cluster_solutions_monitor_v2().
2. Replace every AuditEvent details={...} with
   details=_make_audit_details(mode, run_id, round=..., key=value, ...).
3. Remove any remaining inline "event_version": "v2" dicts.

FILES TO MODIFY:
- cloud/hivemind_core/debate.py

TESTS TO ADD:
- Test that every audit event in a run's output.audit_trail has
  event_version, mode, run_id, and timestamp_iso in its details dict.

===============================================================================
2) REMAINING WORK — AUTH ENFORCEMENT ON API ROUTES
===============================================================================

STATUS: get_current_user() and get_current_client() JWT dependency functions
exist in cloud/app/deps.py but are not applied to any router endpoint. All
routes remain unprotected.

REQUIREMENT:
Apply JWT auth dependencies to all API routes:
- Admin-facing routes (agents, knowledge_bases, simulations, settings,
  scraped_sources, client_data, clients): Depends(get_current_user)
- Client-facing routes (analysis, auth/client-connect, sync): Depends(get_current_client)
- Public/exempt routes: GET /health, POST /auth/login, GET /admin/ping

FIX:
1. For each router file in cloud/app/routers/, add the appropriate Depends()
   to every endpoint function signature.
2. Ensure the auth router's login endpoint remains unauthenticated.
3. Update the client and admin API clients to include the JWT token in the
   Authorization: Bearer header on every request.

FILES TO MODIFY:
- cloud/app/routers/agents.py
- cloud/app/routers/knowledge_bases.py
- cloud/app/routers/simulations.py
- cloud/app/routers/settings.py
- cloud/app/routers/analysis.py
- cloud/app/routers/clients.py
- cloud/app/routers/client_data.py
- cloud/app/routers/scraped_sources.py
- cloud/app/routers/sync.py
- admin/src/api/client.ts (add Authorization header)
- client/src/api/client.ts (add Authorization header)

TESTS TO ADD:
- Test that unauthenticated requests to protected endpoints return 401.
- Test that requests with valid JWT succeed.

===============================================================================
3) REMAINING WORK — CLIENT DENSITY SLIDER USES SERVER BOUNDS
===============================================================================

STATUS: The server endpoint GET /knowledge-bases/density-bounds exists and
returns { min_doc_tokens, sum_all_doc_tokens }. However, the client app does
not call this endpoint. The density slider in client/src/App.tsx is still
hardcoded with min=1000, max=50000.

REQUIREMENT:
After login and agent sync, the client should fetch density bounds from the
server and use them for the density slider's min/max. Fall back to hardcoded
values if the endpoint fails or returns no data.

FIX:
1. In client/src/api/client.ts, add a getDensityBounds() method that calls
   GET /knowledge-bases/density-bounds.
2. In client/src/App.tsx, fetch density bounds after login/agent sync.
   Store in state. Use as slider min/max, falling back to current defaults.

FILES TO MODIFY:
- client/src/api/client.ts
- client/src/App.tsx

===============================================================================
4) REMAINING WORK — CLIENT DATA MANAGEMENT
===============================================================================

STATUS: Not implemented. The client provides a single text area for pasting
context. There is no structured data management or persistent client data store.
Backend has ClientData model and routes but no end-to-end integration.

REQUIREMENT (Phase A — Client-Cleared Data):
1. In the client app, add a "My Data" panel where the user can upload documents
   or paste text snippets. These persist across sessions via the server.
2. CRUD operations via existing /clients/{id}/data endpoints.
3. When running an analysis, automatically include all client-cleared data as
   context items (in addition to the query and any pasted text).

REQUIREMENT (Phase B — Internet Data, lower priority):
1. Add a web scraping service that can fetch and extract text from URLs.
2. Integrate into analysis pipeline so agents can reference internet data.
3. Rate limiting, caching, and content filtering for safety.

FILES TO MODIFY:
- client/src/App.tsx (add My Data panel)
- client/src/api/client.ts (add client data CRUD methods)
- cloud/app/routers/client_data.py (verify endpoints work end-to-end)

===============================================================================
5) REMAINING WORK — INVARIANT HARDENING
===============================================================================

STATUS: Invariants I4 and I5 are mostly enforced but not explicitly guarded
with assertions. I6 (streaming/non-streaming equivalence) needs a proper test
now that streaming uses the thread+queue pattern.

I4. If recommendation.status == "approved", average_feasibility > threshold.
    The finalization logic in run_simple_mode and run_full_mode does enforce
    this, but add an explicit assertion/guard at the end of each function
    that re-checks every approved recommendation.

I5. If recommendation.status == "failed_after_repairs", repair_history length
    == max_repair_iterations (or iterations performed before budget exhaustion).
    Verify the repair loop's for-else pattern guarantees this and add a test.

I6. Streaming output must be semantically equivalent to non-streaming output.
    Add a test that runs both run_debate() and run_debate_streaming() with the
    same MockLLM input and compares the final HivemindOutput.

FIX:
1. In run_simple_mode and run_full_mode, after building the surviving list,
   add: assert all(r.average_feasibility > threshold for r in surviving
   if r.status == RecommendationStatus.APPROVED.value)
2. Add explicit tests for I4, I5, and I6.

FILES TO MODIFY:
- cloud/hivemind_core/debate.py (add guards)
- cloud/tests/test_debate_modes.py (add invariant tests)

===============================================================================
6) REMAINING WORK — INLINE HTML DASHBOARDS EXTRACTION
===============================================================================

STATUS: main.py contains ~300+ lines of inline HTML for the server dashboard
and knowledge browser. The templates directory exists but templates are
rendered inline.

REQUIREMENT:
Move all inline HTML to Jinja2 template files and serve them properly:
1. Move dashboard HTML to cloud/app/templates/dashboard.html
2. Move knowledge browser HTML to cloud/app/templates/knowledge_browser.html
3. Use Jinja2Templates from FastAPI/Starlette to render them
4. Remove inline HTML strings from main.py

FILES TO MODIFY:
- cloud/app/main.py
- cloud/app/templates/dashboard.html (create)
- cloud/app/templates/knowledge_browser.html (create)

===============================================================================
7) REMAINING WORK — SIMULATION TOOL-USE DURING DEBATE
===============================================================================

STATUS: Theory agents receive simulation formulas as part of their system
prompt but can only describe what a simulation would produce — they cannot
actually execute simulations mid-response. The execute_agent function does
handle tool_calls in the LLM response, but this relies on the LLM actually
invoking tools, which requires Anthropic's tool-use API feature to be properly
wired.

REQUIREMENT:
Verify or implement that simulation formulas are actually callable as tools
during agent execution:
1. Confirm simulations_to_tools() produces valid Anthropic tool definitions
2. Confirm the LLM adapter passes tools correctly to the API call
3. Confirm tool_calls in the response are properly parsed and executed
4. Add a test with MockLLM that returns a tool_call and verify run_simulation
   is invoked and results appended

FILES TO VERIFY/MODIFY:
- cloud/hivemind_core/agents.py (execute_agent tool handling)
- cloud/hivemind_core/simulations.py (simulations_to_tools)
- cloud/hivemind_core/llm.py (ClaudeAdapter tool passing)
- cloud/tests/ (add tool-use integration test)

===============================================================================
8) REMAINING WORK — DYNAMIC UNIT RAG SCOPING
===============================================================================

STATUS: _dynamic_unit_to_agent() creates agents with document_ids set from
the DynamicTheoryUnit's assigned documents. The VectorStoreInterface.retrieve()
accepts an optional document_ids filter. However, the Qdrant adapter
(cloud/app/adapters/vector_db.py and cloud/hivemind_core/adapters/qdrant_vector_store.py)
must actually implement this filter for scoped retrieval to work.

REQUIREMENT:
Verify that when document_ids is passed to retrieve(), only chunks belonging
to those specific documents are returned from Qdrant.

FIX:
1. Check cloud/app/adapters/vector_db.py — does it filter by document_ids
   in the Qdrant query payload? If not, add a Must condition filtering
   point.payload.document_id in the provided list.
2. Same check for cloud/hivemind_core/adapters/qdrant_vector_store.py.
3. Add a test verifying that scoped retrieval returns only chunks from
   the specified documents.

FILES TO VERIFY/MODIFY:
- cloud/app/adapters/vector_db.py
- cloud/hivemind_core/adapters/qdrant_vector_store.py
- cloud/app/rag/vector_store.py (if retrieval is routed through here)

===============================================================================
9) REMAINING WORK — TEST COVERAGE GAPS
===============================================================================

STATUS: Current test suites cover types, debate helpers, debate modes, and
simulations. Missing coverage for:

A) Canonical form pipeline:
   - _extract_canonical_form with valid JSON, malformed JSON, budget exhaustion
   - _detect_contradiction with contradicting/non-contradicting pairs
   - _merge_canonical_forms with multiple forms
   - cluster_solutions_monitor_v2 end-to-end with canonical merge

B) Critique/revision pipeline:
   - _parse_critique_response with well-formed and malformed sections
   - _create_revision_prompt with varying revision_strength values
   - Full critique+revision round in run_full_mode

C) Dynamic unit creation:
   - _create_dynamic_units with varying document counts and density values
   - Edge cases: single document > density, many small documents, empty input

D) Practicality scoring:
   - apply_practicality_scoring partial scoring (BudgetExhausted mid-scoring)
   - practicality_criticality prompt injection verification

E) Auth enforcement (once Section 2 is implemented):
   - Protected endpoint returns 401 without token
   - Valid operator token grants access to admin routes
   - Valid client token grants access to client routes
   - Client token rejected on admin routes and vice versa

FILES TO CREATE/MODIFY:
- cloud/tests/test_canonical_forms.py (new)
- cloud/tests/test_critique_revision.py (new)
- cloud/tests/test_dynamic_units.py (new)
- cloud/tests/test_practicality.py (new)
- cloud/tests/test_auth.py (new, after Section 2)

===============================================================================
10) ACCEPTANCE CRITERIA — CHECKLIST
===============================================================================

[DONE] AC1. Client defaults to simple mode and hides complex knobs by default.
[DONE] AC2. Full blind global restart is no longer default behavior.
[DONE] AC3. Recommendation-level repair loop works and is audited.
[DONE] AC4. Budget + stagnation stop conditions proven in tests.
[DONE] AC5. Monitor v2 emits transparent cluster evidence in audit.
[DONE] AC6. Simple vs full benchmark report exists and is reproducible.
[DONE] AC7. No regression in existing auth/agent selection/streaming UX.
[TODO] AC8. All audit events use _make_audit_details with consistent base fields.
[TODO] AC9. API routes enforce JWT authentication.
[TODO] AC10. Client density slider uses server-provided bounds.
[TODO] AC11. Invariants I4/I5/I6 have explicit guards and tests.
[TODO] AC12. Test coverage includes canonical forms, critique/revision, dynamic
       units, practicality, and auth.

===============================================================================
11) IMPLEMENTATION OUTPUT REQUIREMENTS
===============================================================================

When implementing, produce:
1) Summary of changes made (per section)
2) Exact files changed + created
3) Test evidence (all tests passing)
4) Any new residual risks

Do not stop at planning; implement end-to-end with tests.

===============================================================================
12) PRIORITY ORDER
===============================================================================

Implement in this order (by impact and dependency):

1. Section 2 — Auth enforcement (security, blocks AC9)
2. Section 1 — Audit event convention (consistency, blocks AC8)
3. Section 5 — Invariant hardening (safety, blocks AC11)
4. Section 9 — Test coverage gaps (quality, blocks AC12)
5. Section 3 — Client density slider bounds (UX, blocks AC10)
6. Section 8 — Dynamic unit RAG scoping verification (correctness)
7. Section 7 — Simulation tool-use verification (feature completeness)
8. Section 4 — Client data management (feature, phased)
9. Section 6 — Inline HTML extraction (maintainability)
